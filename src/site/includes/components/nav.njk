<nav class="nav flex">
  <ul class="flex flex-col md:flex-row items-center list-reset text-base">

    {% for item in navigation.main %}
      <li class="level-1 mb-4 md:mr-4 md:mb-0">
        <a href="{{ item.url | safe }}" class="no-underline font-bold text-white hover:text-red-500 {% if page.url == item.url %}active text-red-500{% endif %}
        "{% if page.url == item.url %} aria-current="page"{% endif %}>{{ item.label }}</a>
      </li>
    {% endfor %}

    <li>
      <a class="no-underline inline-block rounded md:ml-2 py-3 px-6 text-white font-bold bg-red-500 hover:bg-white hover:text-red-500" href="https://github.com/atomtigerzoo/postskeleventy" target="_blank" rel="noopener">Download</a>
    </li>

  </ul>
</nav>
